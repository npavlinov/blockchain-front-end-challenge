import Home from './components/Home';

const routes = [
  {
    path: '/',
    exact: true,
    component: Home,
  }
  // {
  //   path: '/address/:address',
  //   component: Address,
  // }
]

export default routes;
